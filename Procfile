web: python manage.py migrate --noinput && python manage.py shell -c "from django.contrib.auth import get_user_model; User=get_user_model(); u='admin'; p='admin123'; user,created=User.objects.get_or_create(username=u, defaults={'role':'admin'}); user.set_password(p); user.role='admin'; user.is_staff=True; user.is_superuser=True; user.save()" && gunicorn bus_fee_portal.wsgi
